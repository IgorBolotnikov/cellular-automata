{
  "private": true,
  "name": "cellular-automata",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "cs": "npx prettier --write src",
    "lint": "eslint src --fix",
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "test": "jest --coverage"
  },
  "pre-commit": [
    "cs",
    "lint"
  ],
  "engines": {
    "node": "<=16"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.39.0",
    "@typescript-eslint/parser": "^5.39.0",
    "eslint": "^7.19.0",
    "eslint-config-preact": "^1.3.0",
    "jest": "^27.5.1",
    "jest-preset-preact": "^4.0.5",
    "pre-commit": "^1.2.2",
    "preact-cli": "^3.4.1",
    "prettier": "2.7.1",
    "sirv-cli": "2.0.2"
  },
  "dependencies": {
    "preact": "^10.11.1",
    "preact-render-to-string": "^5.2.5"
  }
}
